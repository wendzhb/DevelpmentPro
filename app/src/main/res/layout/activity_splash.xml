<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/detail_download_blue">

    <ImageView
        android:id="@+id/sun"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="90dp"
        android:src="@drawable/sun" />

    <ImageView
        android:id="@+id/cloud_1"
        android:layout_width="120dp"
        android:layout_height="90dp"
        android:layout_below="@+id/sun"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="-50dp"
        android:src="@drawable/cloud"
        android:tint="@color/white" />

    <ImageView
        android:id="@+id/cloud_2"
        android:layout_width="80dp"
        android:layout_height="90dp"
        android:layout_below="@+id/sun"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="-20dp"
        android:layout_marginTop="-50dp"
        android:layout_toEndOf="@+id/cloud_1"
        android:src="@drawable/cloud"
        android:tint="@color/white" />

    <ImageView
        android:id="@+id/cloud_3"
        android:layout_width="90dp"
        android:layout_height="110dp"
        android:layout_below="@+id/sun"
        android:layout_centerHorizontal="true"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="-40dp"
        android:src="@drawable/cloud"
        android:tint="@color/white" />
    <!--原本是黑色，使用tint来让它变成白色，这个属性在实际开发中能帮助我们节省很多内存，因为仅仅是颜色不同的图片，我们也只需要一张。-->

    <com.example.kaifa.dreamran.NormalTextView
        android:id="@+id/join_now"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="180dp"
        android:text="欢迎进入"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:visibility="visible" />

</RelativeLayout>
